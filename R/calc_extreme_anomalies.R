



#do we feed a data set in or vectors? do we need to make replaceable variables
calc_extreme_anomalies <- function(temp, precip){
  #find the max
  max_temp = max(temp$Anomaly)
  min_temp
  max_p
  return(list(hottest = max_temp))

}


